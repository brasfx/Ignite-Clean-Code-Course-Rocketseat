const INTERVAL_EVERY_30_MINUTES = 60 * 30 * 1000;
function lookForUpdades() {}

setInterval(lookForUpdades, INTERVAL_EVERY_30_MINUTES);

function calculateDiscount(priceInCents, discountInPercent) {}
